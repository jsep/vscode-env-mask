# Open questions

Deferred from spec — do not block v1 unless explicitly resolved.

| Question | Default for v1 | Feature impact |
|----------|----------------|----------------|
| Reload persistence for revealed keys? | No persist | 03, 06 |
| Notification on copy vs silent? | Silent | 05 |
| `export` prefix: include in key display or strip? | Key = name after `export` (strip prefix from key) | 02, 04 |

Update this file when a decision is locked; mirror changes in SPEC.md if needed.
